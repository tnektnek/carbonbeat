# is_carbon_beat checks if the data matches the carbon_beat criteria
def is_carbon_beat:
    # TODO: fill out with logic for pipeline include (INTENTIONALLY DOES NOT PASS AS IS)
    .beat.name == "cb"
;
